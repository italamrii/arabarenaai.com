# Public API base URL (must include /v1 suffix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/v1

# Server-only admin credentials (never use NEXT_PUBLIC_ prefix)
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=
